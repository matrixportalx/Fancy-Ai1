# Android Studio / IntelliJ IDEA
.idea/
*.iml
.gradle/
/local.properties
.navigation/

# Build Output
build/
bin/
gen/
/captures
.externalNativeBuild
.cxx/
*.apk
*.aar
*.ap_
*.dex

# Native / C++
app/.cxx/
app/build/

# App Data & Backups (Sensitive)
app/*.zip
app/state.json
app/archive_*.json
app/media_registry.json
app/media/
*.db
*.db-journal

# JetBrains / Android Studio leftover report files
script.js
styles.css
.descriptions.xml
**/inspectionProfiles/

# OS / Misc
.DS_Store
Thumbs.db
*.log
*.bak
*.swp
*~

!gradle-wrapper.jar

# On-device LLM models (downloaded at runtime — never commit GGUF weights)
*.gguf
app/src/main/assets/models/*.gguf

# Editor / agent local config
.claude/
.vscode/

# Vendored OpenCL ICD-loader build output is regenerated; keep the prebuilt lib only
# (CMake links third_party/OpenCL-ICD-Loader/build-android/libOpenCL.so — keep it)

# Leftover signing-experiment backups (the real skels live in jniLibs)
/unsigned_skeletons/
# llama.cpp test-suite vocab fixtures (not needed for the Android build)
app/src/main/cpp/llama.cpp/models/*.gguf
